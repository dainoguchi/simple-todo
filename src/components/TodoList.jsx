import { Button } from "@mui/material";

const TodoList = () => {
  return (
    <>
      <div>
        <h3>Todo List</h3>
        <Button variant="contained">Create Task</Button>
      </div>
    </>
  );
};

export default TodoList;
